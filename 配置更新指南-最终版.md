# 🎉 API配置更新指南 - 最终版

## ✅ 你的配置信息

- **API Key**: `ark-e2ed64ff-2a75-4e93-bb9f-8b5ce8e6034a-eb8e3`
- **Endpoint**: `https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- **模型ID**: `glm-4-7-251222`

---

## 📝 配置更新步骤

### 步骤1：更新 API_CONFIG（第21-25行）

打开文件：`frontend/app.js`

找到第21-25行，将以下代码：

```javascript
// API配置
const API_CONFIG = {
  key: "ff7ec0954b5c4fcd86454a44697d2094.vu2ADm7nHuNDICBx",
  endpoint: "https://ark.cn-beijing.volces.com/api/v3/chat/completions",
};
```

**替换为**：

```javascript
// API配置
const API_CONFIG = {
  key: "ark-e2ed64ff-2a75-4e93-bb9f-8b5ce8e6034a-eb8e3",
  endpoint: "https://ark.cn-beijing.volces.com/api/v3/chat/completions",
  model: "glm-4-7-251222"
};
```

---

### 步骤2：更新 model 参数（第179行）

找到第179行，将以下代码：

```javascript
model: 'ep-20240422143237-5r6d8',
```

**替换为**：

```javascript
model: API_CONFIG.model || 'glm-4-7-251222',
```

---

## 🧪 测试配置

### 方法1：使用测试工具

1. 打开 `test-api.html`
2. 填入配置信息：
   - API Key: `ark-e2ed64ff-2a75-4e93-bb9f-8b5ce8e6034a-eb8e3`
   - Endpoint: `https://ark.cn-beijing.volces.com/api/v3/chat/completions`
   - 模型ID: `glm-4-7-251222`
3. 点击 **"🚀 测试API连接"**
4. 查看测试结果

### 方法2：在项目中测试

1. 更新配置后，打开 `frontend/index.html`
2. 按 F12 打开开发者工具
3. 在控制台查看API调用日志
4. 输入测试消息，观察响应

---

## 📋 完整的配置代码

### 位置1：frontend/app.js 第21-25行

```javascript
// API配置
const API_CONFIG = {
  key: "ark-e2ed64ff-2a75-4e93-bb9f-8b5ce8e6034a-eb8e3",
  endpoint: "https://ark.cn-beijing.volces.com/api/v3/chat/completions",
  model: "glm-4-7-251222"
};
```

### 位置2：frontend/app.js 第179行

```javascript
model: API_CONFIG.model || 'glm-4-7-251222',
```

---

## ✅ 完成检查清单

- [ ] 已打开 `frontend/app.js` 文件
- [ ] 已更新第21-25行的 API_CONFIG
- [ ] 已更新第179行的 model 参数
- [ ] 已保存文件
- [ ] 已使用测试工具验证API连接
- [ ] 测试成功或在项目中验证功能

---

## 🔍 常见问题

### Q1: 测试时出现401错误
**原因**：API Key无效或已过期
**解决**：确认API Key是否正确，是否已过期

### Q2: 测试时出现404错误
**原因**：模型ID不存在
**解决**：确认模型ID `glm-4-7-251222` 是否正确

### Q3: 测试时出现CORS错误
**原因**：浏览器跨域限制
**解决**：
- 这是正常现象
- 项目已有降级机制，会自动使用mock实现
- 正式使用建议创建后端代理

### Q4: API调用失败但功能仍可用
**原因**：降级机制生效
**说明**：项目会自动回退到本地mock实现，保证基本功能可用

---

## 💡 重要提示

1. **保存文件**：修改后记得保存 `frontend/app.js`
2. **测试验证**：使用 `test-api.html` 测试API连接
3. **降级机制**：即使API失败，基本功能仍可用
4. **CORS问题**：浏览器直接调用可能遇到跨域限制，这是正常的

---

## 🎯 现在就开始！

1. 打开 `frontend/app.js`
2. 按照上述步骤更新配置
3. 保存文件
4. 使用 `test-api.html` 测试
5. 或直接打开 `frontend/index.html` 验证功能

**配置完成后，你的项目就可以正常调用API了！**
